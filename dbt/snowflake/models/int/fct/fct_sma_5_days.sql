with close_data as (select trading_date, ticker, close from {{ ref('stg_stock_data') }})
    select trading_date, ticker, close, avg(close) over (partition by ticker order by trading_date rows between 4 preceding and current row) as sma from close_data