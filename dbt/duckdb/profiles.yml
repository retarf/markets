markets_yields:
  outputs:
    dev:
      type: duckdb
      # Same local DuckDB file the ingestion load writes to.
      path: "{{ env_var('YIELD_WAREHOUSE_DB') }}"
      schema: main
      threads: 1
  target: dev
